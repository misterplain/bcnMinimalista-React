//favorites reducer using action types

import { 
    GET_FAVORITES,
    GET_FAVORITES_FAIL,
    ADD_FAVORITE,
    ADD_FAVORITE_FAIL,
    REMOVE_FAVORITE,
    REMOVE_FAVORITE_FAIL,
} from "../actions/types";
